spring.application.name=springboot-blog-rest-api
spring.datasource.url=jdbc:mysql://localhost:3306/myblog
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Spring Security
# as we defined userDetailsService in SecurityConfig class we can comment the bellow properties
# These properties we can use for FormBasedAuthentication and BasicAuthentication. But for InMemoryAuthentication we should use userDetailsService method in SecurityConfig
#spring.security.user.name=deepak
#spring.security.user.password=deepak
#spring.security.user.role=ADMIN